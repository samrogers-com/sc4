"""Placeholder URL config for the social_manager app.

No public routes wired yet — the app is admin-only during Phase 2 skeleton.
URL patterns land alongside view implementations.
"""
from django.urls import path

app_name = 'social_manager'

urlpatterns: list = []
