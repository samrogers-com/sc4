# Sam's Collectibles - eBay Automation Dependencies
#
# Install with: pip install -r requirements.txt --break-system-packages

# HTTP requests (for eBay Browse API + OAuth2)
requests>=2.31.0

# HTML parsing (extract titles and item specifics from descriptions)
beautifulsoup4>=4.12.0

# Fuzzy string matching (inventory comparison)
thefuzz>=0.22.0
python-Levenshtein>=0.25.0    # Optional: speeds up thefuzz significantly
